name: Upload Python Package
on:
  workflow_dispatch: {}
jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version:
        - '3.10'
        - '3.11'
        - '3.12'
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v4
      with:
        python-version: ${{ matrix.python-version }}
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ matrix.python-version }}-${{ hashFiles('**/pyproject.toml')
          }}
        restore-keys: '${{ runner.os }}-pip-${{ matrix.python-version }}-

          '
    - name: Install dependencies
      run: 'python -m pip install --upgrade pip

        pip install pytest pytest-asyncio responses

        cd scrapegraph-py

        pip install -e ".[html]"

        '
    - name: Run mocked tests with coverage
      run: 'cd scrapegraph-py

        python -m pytest tests/test_mocked_apis.py -v --cov=scrapegraph_py --cov-report=xml
        --cov-report=term-missing

        '
  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python
      uses: actions/setup-python@v5
      with:
        python-version: 3.x
    - name: Install dependencies
      run: 'python -m pip install --upgrade pip

        pip install setuptools wheel twine build

        '
    - name: Build and publish
      env:
        TWINE_USERNAME: mvincig11
        TWINE_PASSWORD: ${{ secrets.PYPI_PASSWORD }}
      run: 'git fetch --all --tags

        cd scrapegraph-py

        python -m build

        twine upload dist/*

        '
